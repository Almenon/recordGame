{
  "name": "phaser-game",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "python -m http.server 3000",
    "lint": "eslint js/*.js --ignore-pattern public/js/*.min.js || exit 0",
    "build": "babel js/app.js -d babel"
  },
  "description": "phaser game",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-env": "^1.3.3"
  }
}
